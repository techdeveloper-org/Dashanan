<!-- Generated by Phase 5 pipeline -- do not edit manually -->
```mermaid
flowchart TB
    HOST["Host AI System<br/>gRPC / REST client"]

    subgraph ShapeA["Shape A: Embedded Library (NFR-002, ADR-001)<br/>pip package, in-process, offline-capable (NFR-005)"]
        LIB["Dashanan Python SDK Facade<br/>in-process, no network required"]
        LIB_Z1["In-process LRU (Zone 1)"]
        LIB_STRUCT["Embedded SQLite<br/>(Zones 2/3/4/5/7)"]
        LIB_IDX["hnswlib + SQLite FTS5<br/>(Zone 6)"]
        LIB_Z8["Local Filesystem (Zone 8)"]
        LIB --> LIB_Z1
        LIB --> LIB_STRUCT
        LIB --> LIB_IDX
        LIB --> LIB_Z8
    end

    subgraph ShapeB["Shape B: Sidecar Service (NFR-002, ADR-001)<br/>containerized, multi-tenant"]
        API["dashanan-api<br/>+ orchestrator<br/>Python 3.12, gRPC + FastAPI<br/>(co-located, ADR-011)"]
        ROTW["dashanan-rotation-worker<br/>promote/compress/archive sweep"]
        IDXW["dashanan-index-worker<br/>Zone 6 index maintenance"]
        PROVW["dashanan-provenance-relay<br/>drains provenance WAL"]

        BUS[("Event Bus<br/>Redis Streams (ADR-003)")]
        HOTSTORE[("Hot Store: Redis<br/>Zone 1, idempotency, cache (ADR-005)")]
        PG[("Structured Store: PostgreSQL<br/>Zones 2/3/4/5/7 (ADR-006)")]
        VEC[("Vector Index: Qdrant<br/>Zone 6 ANN, per-tenant (ADR-007)")]
        LEX[("Lexical Index: OpenSearch<br/>Zone 6 BM25 (ADR-008)")]
        OBJ[("Object Store: S3-compatible<br/>Zone 8 archives (ADR-009)")]

        API -->|"Zone 1 get/put"| HOTSTORE
        API -->|"ANN query"| VEC
        API -->|"BM25 query"| LEX
        API -->|"publish events, XADD"| BUS
        BUS -->|"XREADGROUP"| ROTW
        BUS -->|"XREADGROUP"| IDXW
        BUS -->|"XREADGROUP"| PROVW
        ROTW -->|"state transitions"| PG
        IDXW -->|"upsert/delete vectors"| VEC
        IDXW -->|"upsert/delete docs"| LEX
        PROVW -->|"append provenance"| PG
        ROTW -->|"write compressed archives"| OBJ
    end

    HOST -->|"embeds in-process"| LIB
    HOST -->|"gRPC/REST, mTLS+JWT"| API

    classDef container fill:#e8f0fe,stroke:#4285f4
    classDef store fill:#f1f3f4,stroke:#5f6368
    class LIB,API,ROTW,IDXW,PROVW container
    class LIB_Z1,LIB_STRUCT,LIB_IDX,LIB_Z8,BUS,HOTSTORE,PG,VEC,LEX,OBJ store
```

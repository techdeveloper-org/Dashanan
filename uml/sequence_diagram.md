<!-- Generated by Phase 5 pipeline -- do not edit manually -->

```mermaid
sequenceDiagram
    autonumber
    %% === FLOW 1: Context assembly read path (FR-009, FR-006, FR-007) ===
    %% Traces: POST /context/assemble (assembleContext), HLD Section 2 Communication
    %% Matrix ("dashanan-orchestrator -> Vector/Lexical index: sync parallel fan-out of 2"),
    %% Section 3.7 (Zone 6 is derived/RRF-fused), ADR-008 (RRF fusion, k=60).
    participant Host as Host AI System
    participant API as dashanan-api (Orchestrator)
    participant Z1 as Zone 1 Working (Hot Store)
    participant Vec as Zone 6 Vector Index (Qdrant)
    participant Lex as Zone 6 Lexical Index (OpenSearch)
    participant Zn as Zones 2/3/4/5 (Structured Store)

    rect rgb(235,245,255)
    Note over Host,Zn: Flow 1 -- Context Assembly (sync read)
    Host->>API: POST /context/assemble (AssembleContextRequest: session_id, task/query_embedding, token_budget, zones, cursor)
    API->>Z1: Read current-session Working items (read-your-own-write, ADR-002)
    par Zone 6 hybrid fan-out (RRF-fused)
        API->>Vec: ANN query (vector similarity)
        Vec-->>API: Ranked candidate list (vector_only)
    and
        API->>Lex: BM25 query (lexical)
        Lex-->>API: Ranked candidate list (lexical_only)
    end
    API->>API: Reciprocal Rank Fusion (score = sum 1/(k+rank), k=60) -> hybrid_rrf
    API->>Zn: Fetch owning-zone records for fused item_ids (score_terms, provenance)
    Zn-->>API: MemoryItem records
    API->>API: Budget-fit + rank (token_budget cap, max_items cap, min_provenance_conf)
    API-->>Host: 200 ContextAssembly (items, token_count_total, items_considered, items_returned, degraded, retrieval_mode, next_cursor)
    Note right of Host: If items_considered > items_returned,<br/>next_cursor is echoed for the SAME assembly_id<br/>(AssembleContextRequest.cursor on the follow-up call).<br/>410 StaleCursor if replayed against a rotated/expired basis.
    end
```

```mermaid
sequenceDiagram
    autonumber
    %% === FLOW 2: Memory write path (FR-009, FR-010, FR-013) ===
    %% Traces: POST /memory/write (writeMemory), ADR-002 (sync accept + async process),
    %% ADR-010 (provenance write-ahead journal), x-internal-event-contracts.memory.written
    %% (publisher: Orchestrator; consumers: index-worker, provenance-relay, rotation-worker).
    participant Host as Host AI System
    participant API as dashanan-api (Orchestrator)
    participant Z1 as Zone 1 Working (Hot Store)
    participant Journal as Provenance WAL (local fsync)
    participant Bus as Event Bus (Redis Streams)
    participant Rot as rotation-worker
    participant Relay as provenance-relay
    participant Z7 as Zone 7 Provenance (Structured Store)

    rect rgb(255,245,235)
    Note over Host,Z7: Flow 2 -- Memory Write (sync accept, async process)
    Host->>API: POST /memory/write (WriteMemoryRequest: session_id, content, provenance) + Idempotency-Key
    API->>API: FR-010 boundary check: resolvable source_type? (422 if not)
    API->>Z1: Synchronous Zone 1 write (durability barrier, ADR-002)
    API->>Journal: fsync provenance record (durability barrier, ADR-010)
    API-->>Host: 202 WriteReceipt (write_id, status=accepted, accepted_at)
    API->>Bus: Publish memory.written (item_id, zone, token_count, provenance_id, score_terms)
    par Async consumers (consumer groups, XREADGROUP)
        Bus->>Rot: Deliver memory.written
        Rot->>Rot: Schedule rotation deadline (promote/compress/archive timer)
    and
        Bus->>Relay: Deliver memory.written
        Relay->>Journal: Drain provenance WAL entry
        Relay->>Z7: Append ProvenanceRecord (prev_hash/record_hash chain, status: durable)
    end
    Note right of Host: Host may poll GET /writes/{write_id}<br/>for status: accepted -> indexed -> failed.
    end
```

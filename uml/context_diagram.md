<!-- Generated by Phase 5 pipeline -- do not edit manually -->
```mermaid
flowchart TB
    subgraph Actors["External Actors (HLD S1)"]
        HOST["Host AI System / Agent Framework<br/>(FR-009)"]
        CODING["Long-running Coding Agent<br/>(host profile)"]
        SUPPORT["Short-lived Support Session<br/>(host profile)"]
        OPERATOR["Deployment Operator<br/>(NFR-009)"]
    end

    DASHANAN["Dashanan Memory Orchestrator<br/>(FR-009, FR-012)<br/>8-zone dynamic memory<br/>orchestration engine"]

    subgraph Deps["External Dependencies (HLD S1)"]
        EMBED["Embedding Provider<br/>(FR-006)"]
        SUMLLM["Summarization LLM<br/>(compress transition, FR-012)"]
        STORE["Storage Backends<br/>Relational / KV / Vector /<br/>Lexical / Object Store<br/>(FR-011 pluggable adapters)"]
    end

    HOST -->|"Assemble context (sync) /<br/>Write memory (async-accepted)<br/>gRPC / REST / in-process"| DASHANAN
    CODING -.->|"host profile of"| HOST
    SUPPORT -.->|"host profile of"| HOST
    OPERATOR -->|"Configure + observe<br/>REST admin API (NFR-009)"| DASHANAN

    DASHANAN -->|"Embed query and items"| EMBED
    DASHANAN -->|"Summarize aged items"| SUMLLM
    DASHANAN -->|"Pluggable adapters (FR-011)"| STORE

    classDef actor fill:#e8f0fe,stroke:#4285f4
    classDef core fill:#fef7e0,stroke:#f9ab00,stroke-width:2px
    classDef dep fill:#f1f3f4,stroke:#5f6368
    class HOST,CODING,SUPPORT,OPERATOR actor
    class DASHANAN core
    class EMBED,SUMLLM,STORE dep
```

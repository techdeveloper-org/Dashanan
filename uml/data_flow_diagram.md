<!-- Generated by Phase 5 pipeline -- do not edit manually -->

```mermaid
flowchart LR
    Client([Client / Host AI System])
    Orchestrator[Memory Orchestrator\ndashanan-orchestrator]
    ZoneWrite[(Zone Write\nWorking/Episodic/etc.\nvia zone ports)]
    Journal[(Provenance Journal\nlocal fsync -- durability barrier)]
    Bus{{Event Bus}}
    RotWorker[Rotation Worker\ndashanan-rotation-worker]
    IdxWorker[Index Worker\ndashanan-index-worker]
    ProvRelay[Provenance Relay]
    ProvZone[(Zone 7\nProvenance / Audit Memory)]

    Client -->|write request| Orchestrator
    Orchestrator -->|route write, compute score_terms| ZoneWrite
    Orchestrator -->|append ProvenanceRecord\nsync fsync barrier| Journal
    Orchestrator -->|publish memory.written\nitem_id, zone, token_count,\nprovenance_id, score_terms| Bus

    Bus -->|consume memory.written| RotWorker
    Bus -->|consume memory.written| IdxWorker
    Bus -->|consume memory.written| ProvRelay

    RotWorker -->|rotation sweep decision\nsee state_diagram.md| RotWorker
    RotWorker -->|publish memory.promoted /\nmemory.compressed / memory.archived\nzone-transition event| Bus

    Bus -->|consume zone-transition event| IdxWorker
    Bus -->|consume zone-transition event| ProvRelay

    ProvRelay -->|durable write, replays from\nlast acknowledged offset| ProvZone
    Journal -.->|tailed by| ProvRelay

    classDef async fill:#eef,stroke:#557;
    class Bus async
```
